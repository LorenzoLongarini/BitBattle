import { Model, DataTypes } from 'sequelize';
//import { SequelizeSingleton } from "";


//const sequelize = SequelizeSingleton.getConnection();

export class games extends Model { }

export const game = 
